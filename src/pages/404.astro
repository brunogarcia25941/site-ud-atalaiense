---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Página não encontrada">
  <section class="min-h-[80vh] flex items-center justify-center text-center px-4 py-24 md:py-40">
    <div class="max-w-2xl w-full">
      
      <h1 class="text-6xl font-impact text-orange-600 opacity-10 leading-tight select-none tracking-tighter">
        404
      </h1>
      
      <div class="relative z-10 mt-8 md:mt-12 space-y-8">
        
        <h2 class="text-3xl md:text-6xl font-impact italic text-white uppercase leading-tight">
          Parece que te <span class="text-orange-500">perdeste</span> <br class="hidden md:block" /> no campo!
        </h2>
        
        <p class="text-zinc-500 text-lg md:text-xl max-w-lg mx-auto leading-relaxed">
          O árbitro apitou e esta jogada não existe. A página que procuras foi movida, removida ou nunca entrou em jogo.
        </p>

        <div class="pt-8 md:pt-12">
          <a 
            href="/" 
            class="inline-block bg-orange-600 hover:bg-orange-500 text-white font-bold px-1 py-5 rounded-2xl transition-all shadow-xl shadow-orange-900/20 text-lg uppercase tracking-wide"
          >
            Voltar ao jogo inicial
          </a>
        </div>
      </div>

    </div>
  </section>
</Layout>

<style>
  /* Adiciona um efeito suave de fade-in ao carregar a página */
  section {
    animation: fadeIn 0.8s ease-out;
  }

  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>