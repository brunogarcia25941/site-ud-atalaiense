---
import Layout from '../layouts/Layout.astro';
import parceirosData from '../data/parceiros.json';

// Agrupar parceiros por categoria se quiseres dar destaques diferentes
const principais = parceirosData.filter(p => p.categoria === 'Principal');
const oficiais = parceirosData.filter(p => p.categoria === 'Oficial');
---

<Layout title="Parceiros" description="Conhece as empresas e instituições que apoiam a União Desportiva Atalaiense e contribuem para o sucesso do nosso projeto desportivo.">
  <section class="max-w-7xl mx-auto px-4 py-12 md:py-20">
    <header class="mb-10 border-l-4 border-orange-500 pl-8">
      <h1 class="text-4xl md:text-7xl font-impact italic text-white uppercase leading-none">
        Nossos <span class="text-orange-500">Parceiros</span>
      </h1>
      <p class="text-zinc-400 mt-6 text-xl max-w-2xl leading-relaxed">
        O sucesso da UD Atalaiense deve-se também ao apoio fundamental das empresas e instituições que acreditam no nosso projeto.
      </p>
    </header>

    {principais.length > 0 && (
      <div class="mb-24">
        <h2 class="text-2xl font-bold text-white mb-10 uppercase tracking-widest text-center opacity-50">Parceiros Principais</h2>
        <div class="flex justify-center">
          {principais.map(p => (
            <a href={p.site} target="_blank" class="bg-zinc-900 border border-zinc-800 p-12 rounded-3xl hover:border-orange-500 transition-all group max-w-sm">
              <img src={p.logo} alt={p.nome} class="h-32 w-auto object-contain group-hover:grayscale-0 transition-all duration-500" />
            </a>
          ))}
        </div>
      </div>
    )}

    <div class="mb-24">
      <h2 class="text-2xl font-bold text-white mb-10 uppercase tracking-widest text-center opacity-50">Parceiros Oficiais</h2>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        {oficiais.map(p => (
          <a href={p.site} target="_blank" class="bg-zinc-900 border border-zinc-800 p-8 rounded-2xl hover:border-orange-500 transition-all group flex items-center justify-center">
            <img src={p.logo} alt={p.nome} class="h-20 w-auto object-contain group-hover:grayscale-0 transition-all duration-500" />
          </a>
        ))}
      </div>
    </div>

    <div class="bg-orange-600 rounded-3xl p-12 text-center text-white">
      <h2 class="text-3xl md:text-5xl font-impact italic uppercase mb-6">Queres ser nosso parceiro?</h2>
      <p class="text-orange-100 text-lg mb-10 max-w-xl mx-auto">Une a tua marca a um clube com história e ajuda-nos a formar os campeões de amanhã.</p>
      <a href="/contactos" class="inline-block bg-black text-white px-10 py-4 rounded-xl font-bold hover:bg-zinc-900 transition-all uppercase tracking-widest">
        Entrar em Contacto
      </a>
    </div>
  </section>
</Layout>