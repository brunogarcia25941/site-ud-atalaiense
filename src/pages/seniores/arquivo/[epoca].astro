---
import Layout from '../../../layouts/Layout.astro';
import jogadoresData from '../../../data/jogadores.json';

export async function getStaticPaths() {
  const epocas = [...new Set(jogadoresData
    .filter(j => j.escalao === 'Seniores')
    .map(j => j.epoca))];

  return epocas.map(epoca => ({
    params: { epoca }
  }));
}

const { epoca } = Astro.params;
const epocaTitulo = epoca?.replace('-', '/') || '';
---

<Layout title={`Séniores ${epocaTitulo}`}>
  <section class="max-w-7xl mx-auto px-4 py-12 text-center">
    <header class="mb-16 border-l-4 border-orange-500 pl-6 text-left">
      <h1 class="text-4xl font-black uppercase">Séniores</h1>
      <p class="text-zinc-400 text-xl">{epocaTitulo}</p>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
      <a href={`/seniores/arquivo/${epoca}/jogadores`} 
         class="group bg-zinc-900 border border-zinc-800 p-12 rounded-2xl hover:border-orange-500 transition-all shadow-xl">
        <span class="text-5xl mb-4 block group-hover:scale-110 transition-transform">⚽</span>
        <h2 class="text-2xl font-bold text-white uppercase italic">Ver Jogadores</h2>
      </a>

      <a href={`/seniores/arquivo/${epoca}/equipa-tecnica`} 
         class="group bg-zinc-900 border border-zinc-800 p-12 rounded-2xl hover:border-orange-500 transition-all shadow-xl">
        <span class="text-5xl mb-4 block group-hover:scale-110 transition-transform">📋</span>
        <h2 class="text-2xl font-bold text-white uppercase italic">Equipa Técnica</h2>
      </a>
    </div>

    <div class="mt-16 text-left">
      <a href="/seniores/arquivo" class="text-orange-500 hover:underline">← Voltar ao Arquivo de Épocas</a>
    </div>
  </section>
</Layout>